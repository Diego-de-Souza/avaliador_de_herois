name: Deploy Frontend to AWS

on:
  push:
    branches:
      - main
      - master

env:
  AWS_REGION: us-east-1
  FRONTEND_HOST: ${{ vars.FRONTEND_HOST }}
  SSH_HOST: ${{ vars.SSH_HOST || vars.FRONTEND_HOST }}
  SSH_USER: ec2-user
  APP_DIR: /var/www/app
  SSH_PORT: ${{ vars.SSH_PORT || '22' }}
  SSH_TIMEOUT: 30

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Validate required secrets and variables
        run: |
          if [ -z "${{ vars.FRONTEND_HOST }}" ]; then
            echo "Error: FRONTEND_HOST variable is not set"
            exit 1
          fi
          if [ -z "${{ secrets.SSH_PRIVATE_KEY }}" ]; then
            echo "Error: SSH_PRIVATE_KEY secret is not set"
            exit 1
          fi
          if [ -z "${{ vars.API_URL }}" ]; then
            echo "Warning: API_URL variable is not set. Using default: https://api.heroesplatform.com.br"
          fi
          echo "All required secrets and variables are set"
          echo "FRONTEND_HOST: ${{ vars.FRONTEND_HOST }}"
          echo "SSH_HOST: ${{ env.SSH_HOST }} (for SSH connection)"
          echo "SSH_PORT: ${{ env.SSH_PORT }}"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Run unit tests
        run: npm run test:ci
        continue-on-error: false
        env:
          CI: true
          API_URL: ${{ vars.API_URL || 'http://localhost:3020/api' }}
          ID_CLIENTE_GOOGLE: ${{ vars.ID_CLIENTE_GOOGLE || '' }}
          SECRET_KEY_GOOGLE: ${{ secrets.SECRET_KEY_GOOGLE || '' }}
          STRIPE_PUBLIC_KEY: ${{ vars.STRIPE_PUBLIC_KEY || '' }}
          ENCRYPTION_KEY: ${{ secrets.ENCRYPTION_KEY || '' }}

      - name: Generate Angular Environment Files
        run: |
          echo "🔧 Gerando arquivos de environment para produção..."
          echo "API_URL: ${{ vars.API_URL || 'https://api.heroesplatform.com.br' }}"
          
          # Verificar se o script existe
          if [ ! -f "scripts/generate-env.js" ]; then
            echo "❌ ERRO: Script scripts/generate-env.js não encontrado!"
            echo "Criando script temporário..."
            cat > scripts/generate-env.js << 'EOF'
            const fs = require('fs');
            const path = require('path');
            
            function generateEnvironment(isProduction = false) {
              const envVars = {
                production: isProduction,
                apiURL: process.env.API_URL || (isProduction ? 'https://api.heroesplatform.com.br' : 'http://localhost:3020'),
                ID_CLIENTE_GOOGLE: process.env.ID_CLIENTE_GOOGLE || '',
                SECRET_KEY_GOOGLE: process.env.SECRET_KEY_GOOGLE || '',
                stripePublicKey: process.env.STRIPE_PUBLIC_KEY || '',
                encryptionKey: process.env.ENCRYPTION_KEY || ''
              };
            
              const content = `export const environment = ${JSON.stringify(envVars, null, 2)};`;
              
              const fileName = isProduction ? 'environment.prod.ts' : 'environment.ts';
              const filePath = path.join(__dirname, '..', 'src', 'environments', fileName);
              
              // Criar diretório se não existir
              const dirPath = path.dirname(filePath);
              if (!fs.existsSync(dirPath)) {
                fs.mkdirSync(dirPath, { recursive: true });
              }
              
              fs.writeFileSync(filePath, content);
              console.log(\`✅ \${fileName} gerado com sucesso!\`);
              console.log(\`🔧 API URL: \${envVars.apiURL}\`);
            }
            
            // Gerar apenas production
            generateEnvironment(true);
            EOF
          fi
          
          # Definir variáveis de ambiente para o script
          export API_URL="${{ vars.API_URL || 'https://api.heroesplatform.com.br' }}"
          export ID_CLIENTE_GOOGLE="${{ vars.ID_CLIENTE_GOOGLE || '' }}"
          export SECRET_KEY_GOOGLE="${{ secrets.SECRET_KEY_GOOGLE || '' }}"
          export STRIPE_PUBLIC_KEY="${{ vars.STRIPE_PUBLIC_KEY || '' }}"
          export ENCRYPTION_KEY="${{ secrets.ENCRYPTION_KEY || '' }}"
          export NODE_ENV="production"
          
          # Executar script
          node scripts/generate-env.js
          
          # Verificar se os arquivos foram criados
          echo "📄 Verificando arquivos gerados..."
          if [ -f "src/environments/environment.prod.ts" ]; then
            echo "✅ environment.prod.ts criado:"
            cat src/environments/environment.prod.ts | grep -A5 "apiURL"
          else
            echo "❌ environment.prod.ts não foi criado!"
            exit 1
          fi
          
          # Verificar também environment.ts (para garantir)
          if [ -f "src/environments/environment.ts" ]; then
            echo "✅ environment.ts existe"
          fi

      - name: Force Replace localhost References
        run: |
          echo "🔍 Procurando e substituindo referências a localhost:3020..."
          
          API_URL="${{ vars.API_URL || 'https://api.heroesplatform.com.br' }}"
          
          # Substituir em todos os arquivos TypeScript/JavaScript/HTML
          find src -type f \( -name "*.ts" -o -name "*.js" -o -name "*.tsx" -o -name "*.jsx" -o -name "*.html" \) \
            -exec grep -l "localhost:3020" {} \; 2>/dev/null | while read file; do
            echo "🔧 Corrigindo: $file"
            sed -i "s|http://localhost:3020|${API_URL}|g" "$file"
            sed -i "s|localhost:3020|${API_URL}|g" "$file"
          done || true  # Ignora erro se não encontrar nada
          
          echo "✅ Substituição concluída"
          
          # Verificação final
          echo "📋 Verificando se ainda há referências a localhost:3020..."
          if grep -r "localhost:3020" src/ --include="*.ts" --include="*.js" --include="*.html" 2>/dev/null; then
            echo "⚠️  Ainda há referências a localhost:3020!"
            grep -r "localhost:3020" src/ --include="*.ts" --include="*.js" --include="*.html" 2>/dev/null | head -5
          else
            echo "✅ Nenhuma referência a localhost:3020 encontrada"
          fi

      - name: Build application
        run: npm run build -- --configuration=production
        env:
          NODE_ENV: production

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          
          # Corrigir formatação da chave (remover \n literais)
          sed -i 's/\\n/\n/g' ~/.ssh/deploy_key
          sed -i 's/\r$//' ~/.ssh/deploy_key
          echo "" >> ~/.ssh/deploy_key
          
          # Configurar SSH
          cat > ~/.ssh/config << EOF
          Host ${{ env.FRONTEND_HOST }}
            HostName ${{ env.SSH_HOST }}
            Port ${{ env.SSH_PORT }}
            User ${{ env.SSH_USER }}
            IdentityFile ~/.ssh/deploy_key
            StrictHostKeyChecking no
            UserKnownHostsFile /dev/null
            ConnectTimeout ${{ env.SSH_TIMEOUT }}
            ServerAliveInterval 60
            ServerAliveCountMax 3
          EOF
          chmod 600 ~/.ssh/config
          
          # Teste de conectividade
          echo "Testing connection to ${{ env.SSH_HOST }}:${{ env.SSH_PORT }}..."
          if timeout 10 bash -c "cat < /dev/null > /dev/tcp/${{ env.SSH_HOST }}/${{ env.SSH_PORT }}" 2>/dev/null; then
            echo "✅ Port ${{ env.SSH_PORT }} is open and reachable"
          else
            echo "❌ Port ${{ env.SSH_PORT }} test failed"
            echo "Verifique o Security Group no AWS Console"
          fi
          
          # Adicionar host conhecido
          if [ -n "${{ env.SSH_HOST }}" ]; then
            ssh-keyscan -p ${{ env.SSH_PORT }} -H "${{ env.SSH_HOST }}" >> ~/.ssh/known_hosts 2>/dev/null || echo "Warning: Could not add host to known_hosts"
            chmod 644 ~/.ssh/known_hosts
          fi

      - name: Test SSH Connection
        run: |
          echo "Testing SSH connection to ${{ env.SSH_HOST }}:${{ env.SSH_PORT }}..."
          
          SSH_CONFIG_PATH="$HOME/.ssh/config"
          SSH_CONNECTION_FAILED=false
          
          if [ -f "$SSH_CONFIG_PATH" ]; then
            echo "✅ SSH config encontrado. Testando conexão..."
            timeout 30 ssh -F "$SSH_CONFIG_PATH" ${{ env.FRONTEND_HOST }} "echo '✅ SSH connection successful'; hostname" || SSH_CONNECTION_FAILED=true
          else
            echo "⚠️  SSH config não encontrado, usando método alternativo..."
            timeout 30 ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -p ${{ env.SSH_PORT }} ${{ env.SSH_USER }}@${{ env.SSH_HOST }} "echo '✅ SSH connection successful'; hostname" || SSH_CONNECTION_FAILED=true
          fi
          
          if [ "$SSH_CONNECTION_FAILED" = true ]; then
            echo ""
            echo "❌ ERROR: Cannot connect to ${{ env.SSH_HOST }}:${{ env.SSH_PORT }}"
            echo ""
            echo "Tente conectar manualmente para diagnóstico:"
            echo "  ssh -v -i ~/.ssh/deploy_key ${{ env.SSH_USER }}@${{ env.SSH_HOST }} -p ${{ env.SSH_PORT }}"
            exit 1
          fi
          
          echo "✅ SSH connection successful!"

      - name: Copy files to EC2
        run: |
          echo "📂 Iniciando cópia de arquivos para o servidor..."
          
          # Verificar se a pasta dist existe
          if [ ! -d "./dist/avaliador_de_herois" ]; then
            echo "❌ ERROR: Pasta dist/avaliador_de_herois não encontrada!"
            exit 1
          fi
          
          if [ -z "$(ls -A ./dist/avaliador_de_herois)" ]; then
            echo "❌ ERROR: Pasta dist/avaliador_de_herois está vazia!"
            exit 1
          fi
          
          echo "✅ Build encontrado com arquivos. Copiando..."
          
          SSH_CONFIG_PATH="$HOME/.ssh/config"
          
          # Usar método mais confiável: tar + scp
          echo "📦 Criando pacote para envio..."
          cd dist && tar czf app.tar.gz avaliador_de_herois/ && cd ..
          
          # Copiar para servidor
          if [ -f "$SSH_CONFIG_PATH" ]; then
            echo "Usando SSH config para copiar..."
            scp -F "$SSH_CONFIG_PATH" ./dist/app.tar.gz ${{ env.FRONTEND_HOST }}:/tmp/app.tar.gz
          else
            echo "Usando método alternativo para copiar..."
            scp -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -P ${{ env.SSH_PORT }} ./dist/app.tar.gz ${{ env.SSH_USER }}@${{ env.SSH_HOST }}:/tmp/app.tar.gz
          fi
          
          if [ $? -eq 0 ]; then
            echo "✅ Pacote copiado com sucesso"
            rm -f ./dist/app.tar.gz
          else
            echo "❌ Falha ao copiar pacote"
            exit 1
          fi
          
          # Script para descompactar no servidor
          EXTRACT_SCRIPT='
          echo "📦 Descompactando pacote no servidor..."
          
          # Criar diretório temporário
          sudo mkdir -p /tmp/app_new
          
          # Extrair arquivos
          sudo tar xzf /tmp/app.tar.gz -C /tmp/app_new --strip-components=1
          
          # Verificar se extraiu corretamente
          if [ -z "$(ls -A /tmp/app_new)" ]; then
            echo "❌ ERRO: Nenhum arquivo extraído!"
            exit 1
          fi
          
          echo "✅ Arquivos extraídos: $(ls -la /tmp/app_new | wc -l) itens"
          
          # Criar backup do antigo
          if [ -d "${{ env.APP_DIR }}" ]; then
            sudo mkdir -p ${{ env.APP_DIR }}.backup
            sudo cp -r ${{ env.APP_DIR }}/* ${{ env.APP_DIR }}.backup/ 2>/dev/null || true
            echo "✅ Backup criado em ${{ env.APP_DIR }}.backup"
          fi
          
          # Limpar diretório atual
          sudo mkdir -p ${{ env.APP_DIR }}
          sudo rm -rf ${{ env.APP_DIR }}/*
          
          # Mover novos arquivos
          sudo mv /tmp/app_new/* ${{ env.APP_DIR }}/
          
          # Limpar temporários
          sudo rm -rf /tmp/app_new
          sudo rm -f /tmp/app.tar.gz
          
          echo "✅ Arquivos movidos para ${{ env.APP_DIR }}"
          '
          
          # Executar script no servidor
          if [ -f "$SSH_CONFIG_PATH" ]; then
            ssh -F "$SSH_CONFIG_PATH" ${{ env.FRONTEND_HOST }} "$EXTRACT_SCRIPT"
          else
            ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -p ${{ env.SSH_PORT }} ${{ env.SSH_USER }}@${{ env.SSH_HOST }} "$EXTRACT_SCRIPT"
          fi
          
          if [ $? -eq 0 ]; then
            echo "✅ Arquivos copiados e preparados no servidor"
          else
            echo "❌ Falha ao preparar arquivos no servidor"
            exit 1
          fi

      - name: Configure Nginx and Permissions
        run: |
          echo "🔧 Configurando Nginx e permissões..."
          
          SSH_CONFIG_PATH="$HOME/.ssh/config"
          
          SERVER_SCRIPT='
          echo "🔧 Configurando permissões..."
          
          # Ajustar permissões
          sudo chown -R nginx:nginx ${{ env.APP_DIR }}
          sudo chmod -R 755 ${{ env.APP_DIR }}
          
          echo "✅ Permissões configuradas"
          
          echo "🔧 Configurando Nginx..."
          
          # Criar configuração Nginx
          sudo tee /etc/nginx/conf.d/heroesplatform.conf > /dev/null << "NGINX_EOF"
          server {
              listen 80;
              listen [::]:80;
              server_name ${{ vars.FRONTEND_HOST }} www.${{ vars.FRONTEND_HOST }};
              
              # IMPORTANTE: Angular 17+ compila para browser/ dentro de dist
              root ${{ env.APP_DIR }}/browser;
              index index.html;
              
              # Logs
              access_log /var/log/nginx/heroesplatform-access.log;
              error_log /var/log/nginx/heroesplatform-error.log;
              
              # Gzip
              gzip on;
              gzip_vary on;
              gzip_min_length 1024;
              gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;
              
              # Security headers
              add_header X-Frame-Options "SAMEORIGIN" always;
              add_header X-Content-Type-Options "nosniff" always;
              add_header X-XSS-Protection "1; mode=block" always;
              
              # Angular SPA routing
              location / {
                  try_files $uri $uri/ /index.html;
                  
                  # Cache estáticos
                  location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
                      expires 1y;
                      add_header Cache-Control "public, immutable";
                  }
              }
              
              # Bloquear arquivos ocultos
              location ~ /\. {
                  deny all;
                  access_log off;
                  log_not_found off;
              }
              
              # Proxy para API se necessário
              location /api/ {
                  proxy_pass http://localhost:3020;
                  proxy_http_version 1.1;
                  proxy_set_header Upgrade $http_upgrade;
                  proxy_set_header Connection "upgrade";
                  proxy_set_header Host $host;
                  proxy_set_header X-Real-IP $remote_addr;
                  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                  proxy_set_header X-Forwarded-Proto $scheme;
              }
          }
          NGINX_EOF
          
          echo "✅ Configuração do Nginx criada"
          
          # Testar configuração
          echo "🧪 Testando configuração do Nginx..."
          if sudo nginx -t; then
            echo "✅ Configuração do Nginx válida"
            
            # Recarregar Nginx
            sudo systemctl reload nginx
            echo "✅ Nginx recarregado"
            
            # Verificar se está rodando
            if sudo systemctl is-active --quiet nginx; then
              echo "✅ Nginx está rodando"
            else
              echo "⚠️  Nginx não está rodando, iniciando..."
              sudo systemctl start nginx
            fi
          else
            echo "❌ ERRO: Configuração do Nginx inválida!"
            sudo nginx -t
            exit 1
          fi
          
          echo "🎉 Deploy concluído com sucesso!"
          '
          
          # Executar script
          if [ -f "$SSH_CONFIG_PATH" ]; then
            ssh -F "$SSH_CONFIG_PATH" ${{ env.FRONTEND_HOST }} "$SERVER_SCRIPT"
          else
            ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -p ${{ env.SSH_PORT }} ${{ env.SSH_USER }}@${{ env.SSH_HOST }} "$SERVER_SCRIPT"
          fi

      - name: Health Check
        run: |
          echo "🏥 Verificando saúde da aplicação..."
          sleep 8  # Dar tempo para o Nginx recarregar
          
          # Tentar acessar a aplicação
          for i in {1..5}; do
            echo "Tentativa $i de 5..."
            if curl -f -s -o /dev/null -w "HTTP %{http_code}" http://${{ env.FRONTEND_HOST }}; then
              echo ""
              echo "✅ Aplicação está respondendo!"
              exit 0
            else
              echo " ⏳ Aguardando..."
              sleep 3
            fi
          done
          
          echo "⚠️  Aplicação não respondeu após 5 tentativas"
          echo "Verifique manualmente: http://${{ env.FRONTEND_HOST }}"
          exit 0  # Não falha o workflow, apenas avisa

      - name: Cleanup
        if: always()
        run: |
          rm -f ~/.ssh/deploy_key
          rm -f ~/.ssh/config
          echo "✅ Limpeza concluída"